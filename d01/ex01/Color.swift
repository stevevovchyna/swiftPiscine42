public enum Color : String {
    case hearts = "hearts"
    case diamonds = "diamonds"
    case clubs = "clubs"
    case spades = "spades"
        static public let allColors : [Color] = [hearts, diamonds, clubs, spades]
}
